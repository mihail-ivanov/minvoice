
<%= render :partial => 'help' %>

<%= form_for @invoice_template, url: invoice_template_path(@invoice_template, project_id: @project.identifier) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag l(:button_save) %>
<% end %>
